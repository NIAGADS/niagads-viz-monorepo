
interface InlineGeneBrowserProps {
    flankLength: number;
    geneLength: number;
    startLocation: number;
    endLocation: number;
}

const InlineGeneBrowser = ({}: InlineGeneBrowserProps) => {
    return <></>;
}
